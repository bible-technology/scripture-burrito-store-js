<h2>"{{variant}}" Variant of Entry {{entry}}/{{revision}}</h2>

<p><b>&gt; <a href="/">Home</a> &gt; <a href="entries?idserver={{uriencode idServer}}">{{idServerName}}</a> &gt; <a href="revisions?idserver={{uriencode idServer}}&entry={{uriencode entry}}">{{entry}}</a> &gt; <a href="variants?idserver={{uriencode idServer}}&entry={{uriencode entry}}&revision={{uriencode revision}}">{{revision}}</a></b></p>

{{#if variant}}
<table>
<tr>
<th>name</td>
<td>{{nameInLang metadata.identification.name metadata.meta.defaultLanguage}}</td>
</tr>
<tr>
<th>description</td>
<td>{{nameInLang metadata.identification.description metadata.meta.defaultLanguage}}</td>
</tr>
<tr>
<th>abbreviation</td>
<td>{{nameInLang metadata.identification.abbreviation metadata.meta.defaultLanguage}}</td>
</tr>
{{# each metadata.languages}}
<tr>
<th>language</td>
<td>{{this.tag}}</td>
</tr>
{{/each}}
</table>
<p><a href="metadata?idserver={{uriencode idServer}}&entry={{uriencode entry}}&revision={{uriencode revision}}&variant={{uriencode variant}}">View Raw Metadata</a></p>

{{else}}

<p>Yes we have no metadata for {{idServer}} / {{entry}} / {{revision}} / {{variant}}!</p>
{{/if}}
